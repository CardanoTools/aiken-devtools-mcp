# Example environment variables for embedding providers
# Copy this to .env and fill in keys/urls as needed before running ingestion/indexing

# OpenAI
OPENAI_API_KEY=
# Optional: override model or API base URL
# EMBED_OPENAI_MODEL=text-embedding-3-small
# EMBED_OPENAI_URL=https://api.openai.com/v1/embeddings

# Anthropic
ANTHROPIC_API_KEY=
# EMBED_ANTHROPIC_MODEL=claude-embed-1
# EMBED_ANTHROPIC_URL=https://api.anthropic.com/v1/embeddings

# Cohere
COHERE_API_KEY=
# EMBED_COHERE_MODEL=embed-english-v2.0
# EMBED_COHERE_URL=https://api.cohere.ai/v1/embed

# Generic HTTP adapter (POST { input } -> { embedding: [...] } or { data: [{ embedding: [...] }] })
# EMBED_PROVIDER_URL=
# EMBED_PROVIDER_API_KEY=
# EMBED_PROVIDER_HEADER=Authorization

# Priority order of providers (comma separated). If unset, auto-detects based on available keys.
# EMBEDDING_PROVIDERS=openai,cohere,anthropic

# Allow pseudo embeddings as fallback (default: true)
# ALLOW_PSEUDO_EMBEDDINGS=true

# Embedding vector dimension (optional)
# EMBEDDING_DIM=512
